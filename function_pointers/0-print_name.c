#include <function_pointers.h>
/*
 *print_name - Is function in c.
 *
 *@name: Get name form main.
 *
 *@f: Is getting function
 */
void print_name(char *name, void (*f)(char *))
{
	if (z == NULL || f == NULL)
		return;
	char *z = name;
	f(char *z);
}
